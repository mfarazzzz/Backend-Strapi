# ------------------------------
# SERVER CONFIGURATION
# ------------------------------
# Host should be 0.0.0.0 to bind to all interfaces (required for VPS/Docker)
HOST=0.0.0.0
PORT=1337

# CRITICAL: The public URL where Strapi is accessible via Nginx
# Used for Admin Panel assets, Media Library links, and OAuth redirects.
PUBLIC_URL=https://api.rampur.cloud
STRAPI_PUBLIC_URL=https://api.rampur.cloud

# ------------------------------
# SECURITY SECRETS (MANDATORY)
# ------------------------------
# These MUST be consistent across restarts.
# If these change (e.g. random generation on boot), sessions and API tokens WILL fail.
# Generate new values using: `openssl rand -base64 32`

# APP_KEYS: Array of 4 secrets used to sign session cookies.
# If this changes, all users are logged out.
APP_KEYS=generate_key_1,generate_key_2,generate_key_3,generate_key_4

# API_TOKEN_SALT: Used for API Tokens.
API_TOKEN_SALT=generate_random_salt

# ADMIN_JWT_SECRET: Signs the Admin Panel JWT.
# If this changes, Admin Panel throws "Forbidden" or "Invalid Token".
ADMIN_JWT_SECRET=generate_random_secret

# JWT_SECRET: Signs the End-User (Users-Permissions) JWT.
JWT_SECRET=generate_random_secret

# TRANSFER_TOKEN_SALT: Used for Data Transfer features.
TRANSFER_TOKEN_SALT=generate_random_salt

# ENCRYPTION_KEY: Encrypts sensitive database fields.
# If missing, Strapi may fail or use a weak default.
ENCRYPTION_KEY=generate_random_key_32_chars_min

# ------------------------------
# DATABASE (SQLite)
# ------------------------------
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# ------------------------------
# CORS & SECURITY
# ------------------------------
# Allowed origins for CORS requests (Your Frontend URL)
CORS_ORIGINS=https://rampurnews.com,https://www.rampurnews.com

# ------------------------------
# UPLOAD SETTINGS
# ------------------------------
# Max file size in bytes (default 50MB)
UPLOAD_SIZE_LIMIT=52428800
